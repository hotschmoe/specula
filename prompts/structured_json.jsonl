{"prompt": "Output a JSON object describing a person with fields: name, age, email, address (with street, city, zip). Respond with ONLY the JSON.\n\n{\n  \"name\": "}
{"prompt": "Output a JSON array of 5 book objects, each with fields: title, author, year, genre, pages. Respond with ONLY the JSON.\n\n[\n  {\n    \"title\": "}
{"prompt": "Output a JSON object representing the HTTP response for GET /api/users/42, with fields: status, headers, body. body should be a user object with id, username, email, created_at. Respond with ONLY the JSON.\n\n{\n  \"status\": 200,\n  \"headers\": "}
{"prompt": "Output a JSON configuration for a web server with fields: host, port, tls (with cert_path, key_path, min_version), routes (array of path+handler objects), middleware (array of strings). Respond with ONLY the JSON.\n\n{\n  \"host\": "}
{"prompt": "Output a JSON schema for validating a blog post object with title (string, required), body (string, required), tags (array of strings), author_id (integer, required), published_at (string, ISO8601 date-time). Respond with ONLY the JSON.\n\n{\n  \"$schema\": \"http://json-schema.org/draft-07/schema#\",\n  \"type\": \"object\",\n  \"properties\": "}
{"prompt": "Output a JSON package.json for a Node.js CLI tool named 'greet' with a bin entry, dependencies on commander and chalk, and test/lint scripts. Respond with ONLY the JSON.\n\n{\n  \"name\": \"greet\",\n  \"version\": "}
{"prompt": "Output a JSON object representing a GeoJSON FeatureCollection with 3 Point features, each with id, geometry (coordinates as [lng, lat]), and properties (name, category). Respond with ONLY the JSON.\n\n{\n  \"type\": \"FeatureCollection\",\n  \"features\": "}
{"prompt": "Output a JSON OpenAPI 3.0 fragment describing a single GET /products/{id} endpoint, including parameters, 200 response schema with id/name/price, and 404 error response. Respond with ONLY the JSON.\n\n{\n  \"paths\": {\n    \"/products/{id}\": "}
{"prompt": "Output a JSON array of 5 Git commit objects, each with fields: sha (40-char hex), author (name+email), date (ISO8601), message, files_changed (array). Respond with ONLY the JSON.\n\n[\n  {\n    \"sha\": "}
{"prompt": "Output a JSON object describing a Kubernetes Pod manifest with apiVersion, kind, metadata (name+labels), spec (containers with name/image/ports/env/resources). Respond with ONLY the JSON.\n\n{\n  \"apiVersion\": \"v1\",\n  \"kind\": \"Pod\",\n  \"metadata\": "}
